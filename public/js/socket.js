if (getCookie('id')) {
		
	/*const socket = io.connect("http://vendorteam.ru:5050");
	socket.emit('user_online', getCookie('id'));
	
	
	socket.on('set_online_users', (users) => {
		$.each(users, function(socket, userId) {
			$('[userid="'+userId+'"]').find('.staticuser__stat').addClass('online');
		});
	});
	
	
	socket.on('set_online_user', (userId) => {
		$('[userid="'+userId+'"]').find('.staticuser__stat').addClass('online');
	});
	
	
	socket.on('set_offline_user', (userId) => {
		$('[userid="'+userId+'"]').find('.staticuser__stat').removeClass('online');
	});
	
	
	
	
	socket.on('open_popup', () => {
		notify('Это оповещение вызвано из админки с помощью реалтайм технологий!', 'info', 300);
	});*/
	
	
	
}